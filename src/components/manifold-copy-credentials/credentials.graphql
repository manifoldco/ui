query ResourceWithCredentials($resourceLabel: String!) {
  resource(label: $resourceLabel) {
    credentials(first: 100) {
      edges {
        node {
          key
          value
        }
      }
    }
  }
}

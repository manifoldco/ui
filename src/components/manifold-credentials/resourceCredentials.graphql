query ResourceCredentials($resourceLabel: String!) {
  resource(label: $resourceLabel) {
    credentials(first: 25) {
      edges {
        node {
          key
          value
        }
      }
    }
  }
}

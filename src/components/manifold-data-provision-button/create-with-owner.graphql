mutation CreateResourceWithOwner($planId: ID!, $productId: ID!, $regionId: ID!, $resourceLabel: String!, $ownerId: ProfileIdentity!) {
  createResource(
    input: {
      displayName: $resourceLabel
      label: $resourceLabel
      planId: $planId
      productId: $productId
      regionId: $regionId
      owner: $ownerId
    }
  ) {
    data {
      id
      label
    }
  }
}

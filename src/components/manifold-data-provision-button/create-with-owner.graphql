mutation CreateResourceWithOwner($planId: ID!, $productId: ID!, $regionId: ID!, $resourceLabel: String!, $owner: ProfileIdentity!) {
  createResource(
    input: {
      displayName: $resourceLabel
      label: $resourceLabel
      planId: $planId
      productId: $productId
      regionId: $regionId
      owner: $owner
    }
  ) {
    data {
      id
      label
    }
  }
}

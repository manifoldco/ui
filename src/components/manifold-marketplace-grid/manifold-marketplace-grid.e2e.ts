describe('manifold-marketplace-grid', () => {
  it('accepts blacklist of products');
  it('accepts whitelist of products');
  it('features specified products');
  it('displays categories by default');
  it('hides categories with hide-categories');
  it('hides categories when searching');
  it('searches for services');
  it('displays template cards by default');
  it('hides template cards with hide-custom');
});

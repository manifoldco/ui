query ResourceNoCredentials($resourceLabel: String!) {
  resource(label: $resourceLabel) {
    plan {
      product {
        displayName
      }
    }
    owner {
      platform {
        domain
      }
    }
  }
}

query Product($productLabel: String!) {
  product(label: $productLabel) {
    documentationUrl
    supportEmail
    displayName
    images
    label
    logoUrl
    categories {
      label
    }
    termsUrl
    valueProps {
      header
      body
    }
    tagline
    provider {
      displayName
    }
  }
}

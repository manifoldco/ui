query ResourceCard($resourceLabel: String!) {
  resource(label: $resourceLabel) {
    id
    label
    plan {
      product {
        displayName
        logoUrl
      }
    }
    status {
      label
    }
  }
}

import { storiesOf } from '@storybook/html';

storiesOf('Resource Credentials 🔒', module).add(
  'default',
  () => '<manifold-resource-credentials resource-name="stripe"></manifold-resource-credentials>'
);
